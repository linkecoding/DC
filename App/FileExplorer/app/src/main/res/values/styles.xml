<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:actionModeBackground">@color/colorLightGray</item>
        <!-- ActionMode右边的按钮是一个特殊的CloseButton，分割线与CloseButton的Style有关 -->
        <!-- 删除ActionMode的Divider-->
        <item name="android:actionModeCloseButtonStyle">@null</item>
        <!--修改菜单字体颜色-->
        <item name="android:actionMenuTextColor">@android:color/holo_blue_light</item>
        <item name="actionModeCloseDrawable">@drawable/ic_back_24dp</item>
        <!-- Customize your theme here. -->
        <item name="actionModeCloseButtonStyle">@style/ActionModeNoBack</item>
    </style>

    <style name="PopupWindowAnimStyle">
        <item name="android:windowEnterAnimation">@anim/top_in</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/top_out</item>
        <!-- 指定消失的动画xml -->
    </style>

    <style name="MenuDialogStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <!--除去title-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <!--除去背景色-->
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ActionModeNoBack" parent="Base.Widget.AppCompat.ActionButton.CloseMode">
        <item name="android:visibility">gone</item>
    </style>
</resources>
